/**
 * package.json 包管理器
 *  name : 包名
 *  version : 包版本
 *  main : 主文件
 *  description: "描述"
 *  scripts : 别名
 *  dependencies : 依赖包  ^标识 4.x.x ~表示 4.2.x 没有标识 固定
 *  package-lock.json：指定依赖包的 不需要修改
 * 
 * npm init -y 初始化package.json 默认全部为true
 * npm : node 安装的一个依赖项
 * npm install 包名：安装某个包最新版本
 * npm install 包名@版本：安装某个包指定版本
 * npm uninstall 包名：卸载某个包
 * npm install -g 包名：全局安装某个包
 * 
 */